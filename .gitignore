# Abhängigkeiten
node_modules/

# Next.js Build
.next/
out/

# Umgebungsvariablen (nie committen!)
.env
.env.local
.env*.local

# Lokale Daten (Quelle der Wahrheit im Lokal-Modus) — nur Beispiel-Fixtures sind versioniert
daten/*.json
daten/.videos.lock
!daten/beispiel/

# Python (Scraper)
__pycache__/
*.pyc
.venv/

# System
.DS_Store
*.log
tsconfig.tsbuildinfo
daten/.yt_subs_429_cooldown

# Server-Umgebung (VPS) — nie committen
.env.server
